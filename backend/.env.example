# PostgreSQL Database URL format:
# postgresql://<username>:<password>@<host>:<port>/<database_name>

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smartmandi

# Note: You can also use SQLite for quick local development without installing PostgreSQL:
# DATABASE_URL=sqlite:///./smartmandi.db
